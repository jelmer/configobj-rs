[package]
name = "configobj"
version = "0.1.0"
edition = "2021"
rust-version = "1.74"
description = "Reader and writer for the ConfigObj INI-style configuration file format"
license = "BSD-3-Clause"
repository = "https://github.com/jelmer/configobj-rs"
keywords = ["config", "ini", "configobj", "parser"]
categories = ["config", "parser-implementations"]

[dependencies]

[lints.rust]
missing_docs = "warn"
